<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: © 2026 Tenstorrent AI ULC -->

# Comparison — tt-tnt-1024a → tt-tnt-1024-2ep

Baseline `artifacts/hf-tt-tnt-1024a` vs candidate `artifacts/hf-tt-tnt-1024-2ep`, generated by `scripts/evaluate.py` on 2026-08-18T13:50:30+00:00. Every delta is `candidate − baseline`.

## Matched conditions

| fact | baseline | candidate |
|---|---|---|
| eval window | **512** | **512** |
| `max_position_embeddings` | 512 | 512 |
| training-time eval window | 512 | 512 |
| token array | `artifacts/tokens-v3/val_ids.npy` | `artifacts/tokens-v3/val_ids.npy` |
| prompt set | B | B |
| completions/prompt | 32 | 32 |
| seed | 0 | 0 |

## The noise floor

The floor is the **seed-only control**: two runs identical in everything but the RNG seed. Whatever it moves, a seed moves. Derived at runtime (`derived-live`) from `docs/measurements/behaviour-tt-tnt-v3-vs-tt-tnt-v5-setB.json` and `artifacts/checkpoints-tt-tnt-v3/val_losses.jsonl`, `artifacts/checkpoints-tt-tnt-v5/val_losses.jsonl`, never hardcoded.

- **loss floor:** sd of the seed control's per-checkpoint differences = **0.1944 nats** (mean 0.0413, sign 8/22 negative). The *spread*, not the mean: that control's mean delta is near zero precisely because its sign wanders, so the spread is what a candidate has to beat.
- **the rule:** a delta at or below **1.2x** the floor is `NOT INTERPRETABLE`, *whatever its confidence interval says*. A −0.041 register delta at 1.03x the floor cleared its CI, was written up, and was refuted by exactly this control; two collapse signals at 1.01x and 1.05x were correctly excluded on the same rule.
- **and the mirror-image rule:** a large ratio over a tiny denominator that cannot clear its own paired minimum-detectable difference is `below paired detection`, not a finding. Both gates have to pass.

## Loss at a matched window

Both models measured by the **same instrument, at the same 512-token window, on the same token array, in this run** — which is the one construction under which two losses in this project are known to be commensurable.

| | baseline | candidate | delta |
|---|---:|---:|---:|
| mean loss over `[0, 512)` | 2.7624 | 2.5637 | -0.1987 |

This number has **no seed floor of its own** — the seed control's floor is over training-time trajectories, not over this instrument — so it is reported without a ratio and should be read alongside the trajectory section below rather than on its own.

## Loss trajectory (training-time validation)

Both trajectories are at a **512-token window** (checked, not assumed: a mismatch here is refused outright). 10 shared checkpoint steps.

| statistic | value | note |
|---|---:|---|
| **endpoint delta** (step 10000) | **+0.0395** | the headline: 2.8660 vs 2.8266 |
| average over checkpoints | -0.0478 | *not* the headline — an average over a trajectory where two runs start apart and converge is dominated by the early steps, which once made an effect look 9.7x its true size |
| sd of per-checkpoint deltas | 0.0796 | |
| **sign test** | 7/10 negative, p = 0.34 | preferred for trajectories: independent of spread |
| seed floor's own sign split | 8/22 negative | the floor *changes sign*, which is what makes a unanimous candidate convincing |
| endpoint / floor | **0.20x** | floor sd 0.1944 |
| **label** | **NOT INTERPRETABLE** | both gates applied |

Both the sign test and the mean-vs-sd summary are given because they answer different questions: the sign test says *how consistently* the difference pointed one way, mean-vs-sd says *how large* it was relative to run-to-run variation. A finding wants both.

## Behaviour, signal by signal

Paired by prompt over frozen set **B**. The sets are never pooled — `score_behaviour.paired_differences` refuses a cross-set pair outright, and this tool scores both models on one set by construction.

| signal | better | baseline | candidate | delta | 95% CI | min. detectable | seed floor | ratio | **label** |
|---|---|---:|---:|---:|---|---:|---:|---:|---|
| genre collapse rate (any marker) | lower | 0.0681 | 0.0542 | -0.0139 | [-0.0340, +0.0062] | 0.0201 | 0.0542 | 0.26x | **NOT INTERPRETABLE** |
| — story-frame collapse | lower | 0.0146 | 0.0104 | -0.0042 | [-0.0119, +0.0035] | 0.0077 | 0.0035 | 1.20x | **NOT INTERPRETABLE** |
| — lexical-habit collapse | lower | 0.0576 | 0.0500 | -0.0076 | [-0.0238, +0.0085] | 0.0162 | 0.0563 | 0.14x | **NOT INTERPRETABLE** |
| termination rate (</s>) | higher | 0.0542 | 0.0250 | -0.0292 | [-0.0529, -0.0055] | 0.0237 | 0.0146 | 2.00x | **worse** |
| 4-gram repeat rate | lower | 0.0292 | 0.0323 | 0.0030 | [-0.0093, +0.0154] | 0.0124 | 0.0022 | 1.37x | **below paired detection** |
| longest repeated span (words) | lower | 3.5083 | 3.6458 | 0.1375 | [-0.2399, +0.5149] | 0.3774 | 0.0576 | 2.39x | **below paired detection** |
| tinystories margin (nats/word) | lower | -0.5269 | -0.5492 | -0.0224 | [-0.0887, +0.0440] | 0.0663 | 0.0745 | 0.30x | **NOT INTERPRETABLE** |
| nearest source == tinystories | lower | 0.2229 | 0.1854 | -0.0375 | [-0.0763, +0.0013] | 0.0388 | 0.0368 | 1.02x | **NOT INTERPRETABLE** |
| prompt engagement | higher | 0.2067 | 0.2354 | 0.0288 | [-0.0079, +0.0654] | 0.0366 | 0.0066 | 4.34x | **below paired detection** |
| generated tokens before </s> | n/a | 58.7347 | 59.2979 | 0.5632 | [-0.1087, +1.2351] | 0.6719 | 0.3278 | 1.72x | **n/a** |

### Verdict

- **findings (clear the floor AND the paired interval):** termination rate (</s>) (worse, 2.00x)
- **at or below 1.2x the seed floor — NOT INTERPRETABLE whatever their intervals say:** genre collapse rate (any marker) (0.26x), — story-frame collapse (1.20x), — lexical-habit collapse (0.14x), tinystories margin (nats/word) (0.30x), nearest source == tinystories (1.02x)
- **above the floor but inside their own paired interval:** 4-gram repeat rate, longest repeated span (words), prompt engagement
- **no floor available for:** none

## What would refute this

- A **seed replicate** of either arm. Every claim above rests on one run per arm; the floor is one seed pair, and a second pair could widen it.
- A **matched-window** re-measurement of anything quoted here from a different window. Where the two models' training windows differ, the trajectory comparison is refused rather than estimated, and that refusal is not a formality.
- The other frozen prompt set. A behavioural effect that appears on set B and not on set A is a fact about the sets as much as about the models.
