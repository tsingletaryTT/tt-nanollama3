<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: © 2026 Tenstorrent AI ULC -->

# External benchmarks — `tt-tnt-v3`

Model `/home/ttuser/code/tt-tnt/artifacts/hf-tt-tnt-v3`, evaluated on CPU by EleutherAI's `lm-evaluation-harness` 0.4.9 at a 2048-token context, dtype float32, batch size 16. Generated by `scripts/benchmark_external.py`.

## Why this exists

Every other measurement in this repository is self-referential: validation loss on a tail of our own blend, per-source loss on that same blend, behaviour scores on two prompt sets we wrote ourselves, and a noise floor derived from our own seed-only control. A model that had learned to imitate our corpus and nothing else would score exactly as well on all of it. **These numbers are the first this project has that our corpus did not produce** — someone else's benchmarks, someone else's data, someone else's scoring code.

**Chance-level scores were the prediction, not a disappointment.** Read the next section before reading any number below.

## The model, and the gap to its reference class

| | this model | GPT-2 small |
|---|---:|---:|
| parameters | 22,025,088 | 124,439,808 |
| training tokens | unknown | ~40,000,000,000 (WebText, ~40 GB) |
| context window | 2048 | 1,024 |
| layers x hidden | 6 x 384 | 12 x 768 |

Training tokens could not be established: no train.log at /home/ttuser/code/tt-tnt/artifacts/checkpoints-tt-tnt-v3/train.log.

GPT-2 small has **5.6x** this model's parameters, so capacity is part of the gap here as well as data. This model's training-token count could not be established from disk (see above), so the data gap can only be stated for GPT-2's side of it. Every score below should be read against those ratios. A low score here is the expected consequence of the gap and is not evidence that anything is broken.

## How to read this table

- **chance** is what a model that had learned nothing would score. For 4-way multiple choice that is 0.25; for 2-way, 0.50; for open-vocabulary last-word prediction it is ~1/32000, i.e. zero. Perplexities have no chance baseline — there is no "guess at random" strategy for them — and are reported directly.
- **verdict** applies one rule: a score within **2 standard errors** of chance is `AT CHANCE`, and `AT CHANCE` means the number in the score column **is not a measurement of this model**. It is printed so you can see what was suppressed and check the arithmetic, not so it can be quoted. In the companion JSON such a row has `reportable_score: null`.
- This mirrors `scripts/evaluate.py`, which labels any delta inside ~1.2x of the seed-noise floor `NOT INTERPRETABLE` no matter what its confidence interval says. Same principle, different null: there, another run of the same recipe; here, a model that learned nothing.
- **stderr** is lm-eval's own standard error over benchmark items. It covers sampling of the benchmark, and nothing else — not training-seed variance, not prompt-format sensitivity. It is a lower bound on the real uncertainty.
- **The gate is applied per row, and this table has 11 of them.** If every one of those scores were truly at chance, roughly **0.5** would still clear a 2-standard-error gate by luck alone. A row sitting just past the threshold is therefore weaker evidence than its verdict makes it look; a row at 5+ standard errors, or a pattern of several rows moving the same way, is not.

## Results

| task | metric | score | stderr | chance | s.e. from chance | verdict | GPT-2 small |
|---|---|---:|---:|---:|---:|---|---:|
| wikitext | word perplexity | 247.3190 | n/a | — | — | NO CHANCE BASELINE | 37.3695 (measured) |
| wikitext | byte perplexity | 2.8025 | n/a | — | — | NO CHANCE BASELINE | 1.9682 (measured) |
| wikitext | bits per byte | 1.4867 | n/a | — | — | NO CHANCE BASELINE | 0.9769 (measured) |
| lambada_openai | last-word accuracy | 0.0798 | 0.0038 | 0.00 | +21.1 | ABOVE CHANCE | 0.3256 (measured) |
| lambada_openai | perplexity of the final word | 2116.3266 | 117.6677 | — | — | NO CHANCE BASELINE | 40.0552 (measured) |
| hellaswag | accuracy | 0.2584 | 0.0044 | 0.25 | +1.9 | **AT CHANCE** | 0.2892 (measured) |
| hellaswag | length-normalised accuracy | 0.2596 | 0.0044 | 0.25 | +2.2 | ABOVE CHANCE | 0.3114 (measured) |
| piqa | accuracy | 0.5326 | 0.0116 | 0.50 | +2.8 | ABOVE CHANCE | 0.6289 (measured) |
| piqa | length-normalised accuracy | 0.5316 | 0.0116 | 0.50 | +2.7 | ABOVE CHANCE | 0.6251 (measured) |
| winogrande | accuracy | 0.5320 | 0.0140 | 0.50 | +2.3 | ABOVE CHANCE | 0.5162 (measured) |
| arc_easy | accuracy | 0.2963 | 0.0094 | 0.25 | +4.9 | ABOVE CHANCE | 0.4381 (measured) |
| arc_easy | length-normalised accuracy | 0.2950 | 0.0094 | 0.25 | +4.8 | ABOVE CHANCE | 0.3948 (measured) |
| arc_challenge | accuracy | 0.1817 | 0.0113 | 0.25 | -6.1 | BELOW CHANCE | 0.1903 (measured) |
| arc_challenge | length-normalised accuracy | 0.2218 | 0.0121 | 0.25 | -2.3 | BELOW CHANCE | 0.2270 (measured) |
| mmlu | accuracy | 0.2292 | 0.0035 | 0.25 | -5.9 | BELOW CHANCE | 0.2292 (measured) |

## What this says, in words

**At chance** (score not reportable as a quantity): `hellaswag` accuracy.

**Distinguishable from chance:**

- `lambada_openai` last-word accuracy: 0.0798 vs chance 0.00 — 21.1 standard errors above.
  - *Open-vocabulary: the model must produce the exact final word, so chance is ~1/|vocab| (~3e-5 for our 32,000-token vocabulary), i.e. indistinguishable from zero. A nonzero score here is therefore always above chance -- the interesting question for this task is the magnitude, and the comparison to GPT-2.*
  - *GPT-2 small on the same metric: 0.3256 (measured).*
- `hellaswag` length-normalised accuracy: 0.2596 vs chance 0.25 — 2.2 standard errors above.
  - *4-way multiple choice.*
  - *GPT-2 small on the same metric: 0.3114 (measured).*
- `piqa` accuracy: 0.5326 vs chance 0.50 — 2.8 standard errors above.
  - *2-way multiple choice.*
  - *GPT-2 small on the same metric: 0.6289 (measured).*
- `piqa` length-normalised accuracy: 0.5316 vs chance 0.50 — 2.7 standard errors above.
  - *2-way multiple choice.*
  - *GPT-2 small on the same metric: 0.6251 (measured).*
- `winogrande` accuracy: 0.5320 vs chance 0.50 — 2.3 standard errors above.
  - *2-way multiple choice.*
  - *GPT-2 small on the same metric: 0.5162 (measured).*
- `arc_easy` accuracy: 0.2963 vs chance 0.25 — 4.9 standard errors above.
  - *Mostly 4-way multiple choice (a handful of items have 3 or 5 options, so 0.25 is approximate).*
  - *GPT-2 small on the same metric: 0.4381 (measured).*
- `arc_easy` length-normalised accuracy: 0.2950 vs chance 0.25 — 4.8 standard errors above.
  - *GPT-2 small on the same metric: 0.3948 (measured).*
- `arc_challenge` accuracy: 0.1817 vs chance 0.25 — 6.1 standard errors below.
  - *Mostly 4-way multiple choice.*
  - *GPT-2 small on the same metric: 0.1903 (measured).*
- `arc_challenge` length-normalised accuracy: 0.2218 vs chance 0.25 — 2.3 standard errors below.
  - *GPT-2 small on the same metric: 0.2270 (measured).*
- `mmlu` accuracy: 0.2292 vs chance 0.25 — 5.9 standard errors below.
  - *4-way multiple choice.*
  - *GPT-2 small on the same metric: 0.2292 (measured).*

**Graded (no chance floor to sit at):**

- `wikitext` word perplexity: 247.3190 (GPT-2 small: 37.3695, measured)
- `wikitext` byte perplexity: 2.8025 (GPT-2 small: 1.9682, measured)
- `wikitext` bits per byte: 1.4867 (GPT-2 small: 0.9769, measured)
- `lambada_openai` perplexity of the final word: 2116.3266 (GPT-2 small: 40.0552, measured)

## Did the context window truncate anything?

This model's window is **2048 tokens**. When a prompt plus its continuation exceeds it, lm-eval drops tokens off the **front of the context** and the model answers a question it was only shown part of. Every request the harness actually issued was re-tokenized with this model's own tokenizer and checked against the limit.

| task | requests | over the window | longest request (tokens) | fair score? |
|---|---:|---:|---:|---|
| wikitext | 62 | 0 | 16,372 | windowed, not truncated — see note |
| lambada_openai | 5,153 | 0 | 216 | yes |
| hellaswag | 40,168 | 0 | 157 | yes |
| piqa | 3,676 | 0 | 238 | yes |
| winogrande | 2,534 | 0 | 39 | yes |
| arc_easy | 9,501 | 0 | 182 | yes |
| arc_challenge | 4,687 | 0 | 194 | yes |
| mmlu | 56,168 | 0 | 1,112 | yes |

- *wikitext: rolling-loglikelihood task: documents are scored in consecutive 2048-token windows, so nothing is dropped, but every token sees at most 2047 tokens of context*

## Caveats on the GPT-2 column

- A row marked `(measured)` is a real head-to-head: GPT-2 small was run through this same harness — same lm-eval version, same task definitions, same machine, same day — rather than quoted. The one thing it does not match is the context window, which is each model's own; GPT-2 small's is 1024.
- **Protocol cross-check, `wikitext` word perplexity:** measured 37.3695 against the published 37.5000 (0.3% apart) — the two protocols agree closely here.
- **Protocol cross-check, `lambada_openai` last-word accuracy:** measured 0.3256 against the published 0.4599 (29.2% apart) — the two protocols do NOT agree here, so published figures for other rows should be treated with corresponding suspicion.
- **Protocol cross-check, `lambada_openai` perplexity of the final word:** measured 40.0552 against the published 35.1300 (14.0% apart) — the two protocols do NOT agree here, so published figures for other rows should be treated with corresponding suspicion.

- Verified at run time: the `wikitext-2-raw-v1` and `wikitext-103-raw-v1` test splits are byte-identical (1,288,493 characters), so lm-eval's `wikitext` task reports WikiText-103 test perplexity.

## Reproducing this

`lm-eval` is **not** a dependency of this repository and must not become one — `pyproject.toml` lists three runtime dependencies and this project has repeatedly declined to add a fourth. The harness lives in a throwaway virtualenv under `scratch/` (gitignored) and `scripts/benchmark_external.py` shells out to it.

- venv: `/home/ttuser/code/tt-tnt/scratch/lm-eval-venv/bin/python`
- `lm_eval`: 0.4.9
- `torch`: 2.7.0+cpu
- `transformers`: 4.55.4
- `datasets`: 5.0.1
- device: `cpu` (CPU-only by design; this script refuses any other device and never opens a Tenstorrent device)
- harness wall time: 1,425 s
- raw harness output: `/home/ttuser/code/tt-tnt/scratch/lm-eval-runs/tt-tnt-v3/__home__ttuser__code__tt-tnt__artifacts__hf-tt-tnt-v3/results_2026-08-16T16-08-58.843022.json`

```
/home/ttuser/code/tt-tnt/scratch/lm-eval-venv/bin/python -m lm_eval --model hf --model_args pretrained=/home/ttuser/code/tt-tnt/artifacts/hf-tt-tnt-v3,dtype=float32,max_length=2048 --tasks wikitext,lambada_openai,hellaswag,piqa,winogrande,arc_easy,arc_challenge,mmlu --device cpu --batch_size 16 --output_path /home/ttuser/code/tt-tnt/scratch/lm-eval-runs/tt-tnt-v3 --log_samples
```
